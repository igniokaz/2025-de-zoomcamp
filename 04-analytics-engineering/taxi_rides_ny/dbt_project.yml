name: 'taxi_rides_ny'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

models:
  taxi_rides_ny:
    +database: "{{ env_var('DBT_ENV_DATABASE') }}" 
    # +schema: "{{ env_var('DBT_ENV_SCHEMA', '') | trim('_') }}"
    staging:
    core:
      +materialized: table

seeds: 
    taxi_rides_ny:
      +database: "{{ env_var('DBT_ENV_DATABASE') }}" 
      # +schema: "{{ env_var('DBT_ENV_SCHEMA', '') | trim('_') }}"
      taxi_zone_lookup:
          +column_types:
              locationid: numeric

vars:
  payment_type_values: [1,2,3,4,5,6]
  debug_log: "{{ log('Resolved schema is: ' ~ target.schema, info) }}"
  # schema_debug: "{{ log('Resolved TARGET schema: ' ~ target.schema, info) }}"
  schema_debug: "{{ log('DBT_ENV_SCHEMA value: ' ~ env_var('DBT_ENV_SCHEMA', 'NOT SET'), info) }}"